image: node:latest
  
stages:
  - build
  - test
  - build-docker

cache:
  paths:
    - node_modules/

install-dependencies:
  stage: build
  script: yarn
  artifacts:
    paths:
      - node_modules/

run-tests:
  stage: test
  script: yarn run coverage


build-and-push-docker-image:
  stage: build-docker
  only: [master]
  image: docker:git
  services:
    - docker:dind
  before_script:
    - docker login -u $DOCKERHUB_LOGIN -p $DOCKERHUB_PASSWORD
  script:
    - docker pull $DOCKERHUB_REGISTRY_IMAGE:latest || true
    - docker build --cache-from $CI_REGISTRY_IMAGE:latest --tag $DOCKERHUB_REGISTRY_IMAGE:$CI_COMMIT_SHA --tag $DOCKERHUB_REGISTRY_IMAGE:latest .
    - docker push $DOCKERHUB_REGISTRY_IMAGE:$CI_COMMIT_SHA
    - docker push $DOCKERHUB_REGISTRY_IMAGE:latest
